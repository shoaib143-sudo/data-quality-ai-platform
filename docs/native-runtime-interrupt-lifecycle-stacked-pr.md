# Stacked interrupt delivery

This feature is stacked on the durable supervisor resume branch so interrupt terminal behavior can be validated independently before both slices are combined on current main.
