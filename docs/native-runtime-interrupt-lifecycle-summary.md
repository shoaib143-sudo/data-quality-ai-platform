# Governed interrupt lifecycle summary

The implementation makes timeout, rejection, escalation, cancellation, and late-decision handling durable and deterministic while preserving human approval authority. It also closes the prior rejected-decision resume gap.
