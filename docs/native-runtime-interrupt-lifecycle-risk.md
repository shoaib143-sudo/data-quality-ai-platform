# Native runtime interrupt lifecycle risk controls

The implementation intentionally does not add automatic approval, privileged execution, governance-authority changes, or caller-selected timeout behavior. Automated actions are limited to escalation or cancellation after a persisted deadline.
