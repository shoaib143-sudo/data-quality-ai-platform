# Native runtime interrupt lifecycle authority boundaries

Human approval remains authoritative for approval-required actions. Automated timeout handling can only escalate or cancel according to persisted interrupt type; it cannot approve an action, manufacture an approval, alter the action payload hash, or resume execution.
