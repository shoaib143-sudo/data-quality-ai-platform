# Native runtime interrupt lifecycle runbook

If an approval or manual review expires, inspect the append-only terminal action and keep the run non-runnable until a governed follow-up path is chosen. If a dependency or external-input interrupt expires, the run is cancelled. Late decisions are evidence only and never revive the original interrupt.
