# Module 15 CI contract

The dedicated workflow verifies immutable version, decision, and assessment evidence; exact-current-version human approval authority; browser write denial; and no automatic deployment authority from assessments. The global Quality Gate also runs the Module 15 verifier.
