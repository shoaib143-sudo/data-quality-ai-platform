# Native runtime interrupt timeout recovery boundary

An expired interrupt is terminal for that interrupt instance. Recovery may not reinterpret or silently revive it. High-authority timeouts escalate for human handling. Cancellation-policy timeouts terminate the run. Any later recovery must begin from an explicitly governed new run or replay path with its own pinned runtime state.
