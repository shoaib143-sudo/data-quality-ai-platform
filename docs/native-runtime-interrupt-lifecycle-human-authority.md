# Preserved human authority

Approval-required work still needs an authenticated project-admin decision before resume. Automated timeout handling never substitutes for approval and never executes the pending action.
