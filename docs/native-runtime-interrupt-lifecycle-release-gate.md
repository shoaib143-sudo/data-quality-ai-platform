# Native runtime interrupt lifecycle release gate

Release requires the dedicated lifecycle verifier, worker wiring verifier, policy unit checks, TypeScript compilation, production build, repository protected checks, and post-deployment database verification.
