# Operator notes

The manual isolated restore workflow is intentionally disabled unless `run_restore_drill=true` is selected and all recovery secrets point to a deliberately isolated target. Do not configure the recovery target to the production Supabase database.
