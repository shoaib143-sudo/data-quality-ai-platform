# Native runtime interrupt lifecycle CI

The dedicated CI gate runs structural lifecycle verification, scheduled-worker wiring verification, policy unit checks, TypeScript compilation, and the production build.
