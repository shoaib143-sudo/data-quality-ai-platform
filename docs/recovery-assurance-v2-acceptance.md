# Acceptance criteria

The change is acceptable for merge when all mandatory branch checks pass, the Recovery Assurance workflow passes, migration replay remains clean, and the implementation continues to report incomplete scope/timing evidence as non-ready rather than as a successful full-platform recovery.
