# Server-owned timeout policy

`HUMAN_APPROVAL` and `MANUAL_REVIEW` timeout to escalation. `EXTERNAL_INPUT` and `DEPENDENCY` timeout to cancellation. No request field or caller parameter can override this mapping.
