Certification evidence evaluator entry points:

- `node scripts/verify-certification-evidence-evaluator.mjs`
- `node --test scripts/test-certification-evidence-evaluator.mjs`

Both are included in `scripts/ci-full-quality-gate.sh` so claim eligibility semantics are exercised in the full exact-head assurance path.
