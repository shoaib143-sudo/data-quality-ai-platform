import fs from 'node:fs'

function read(path) {
  return fs.readFileSync(path, 'utf8')
}

function requireText(text, needle, label) {
  if (!text.includes(needle)) throw new Error(`AI governance due diligence contract missing: ${label}`)
}

const worker = read('lib/agents/governance-job-worker.ts')
const scheduledWorker = read('app/api/jobs/worker/route.ts')
const directAgentRoute = read('app/api/agents/governance/run/route.ts')
const handoffRoute = read('app/api/agents/governance/handoff/route.ts')
const semanticJobs = read('lib/governance/semantic-jobs.ts')
const fieldLineage = read('supabase/migrations/20260905000510_synthetic_field_lineage_integration_suite.sql')
const contractRename = read('supabase/migrations/20260905000949_rename_contract_specific_evaluator.sql')
const certificationIntelligence = read('supabase/migrations/20260905001538_certification_readiness_and_governance_roi_intelligence.sql')
const metricEngine = read('lib/profiling/metric-engine.ts')
const classificationSync = read('lib/governance/classification.ts')
const reviewProvenance = read('supabase/migrations/20260905003108_governance_knowledge_review_provenance.sql')
const reviewProtection = read('supabase/migrations/20260905003245_protect_human_governance_reviews.sql')
const reviewAtomic = read('supabase/migrations/20260905004617_atomically_audit_governance_knowledge_reviews.sql')
const dueDiligenceGate = read('supabase/migrations/20260905004213_ai_governance_intelligence_due_diligence_gate.sql')
const activationGate = read('supabase/migrations/20260905004511_refine_ai_governance_activation_detection.sql')
const enterpriseKnowledgeApproval = read('supabase/migrations/20260905030354_governed_enterprise_knowledge_document_approval.sql')
const enterpriseRequirementPriority = read('supabase/migrations/20260905030459_align_enterprise_knowledge_requirement_priority.sql')
const reviewRoute = read('app/api/governance/knowledge/review/route.ts')
const enterpriseIngestRoute = read('app/api/governance/knowledge/ingest/route.ts')
const semanticKnowledgeIndexer = read('lib/governance/semantic-knowledge-indexer.ts')
const intelligenceLoader = read('lib/governance/ai-governance-intelligence.ts')

requireText(worker, 'sourceAgentRunId', 'durable governance handoff source provenance')
requireText(worker, 'parent_run_id', 'durable handoff parent run linkage')
requireText(worker, "message_type: 'GOVERNED_HANDOFF'", 'durable handoff message persistence')
requireText(worker, "eventType: 'GOVERNED_AGENT_HANDOFF_COMPLETED'", 'durable handoff governance audit')
requireText(worker, 'await persistRunOutput(result.runId, output)', 'memory-enriched specialist output persistence')
requireText(worker, 'persistGovernedAgentMemoryAndEvaluation', 'durable handoff memory and evaluation persistence')
requireText(worker, 'persistInvestigatorRiskAssessment', 'durable investigator risk persistence')
requireText(worker, 'enrichOutputWithAIGovernanceIntelligence', 'durable agent certification/ROI intelligence enrichment')

requireText(directAgentRoute, 'enrichOutputWithAIGovernanceIntelligence', 'direct agent certification/ROI intelligence enrichment')
requireText(directAgentRoute, ".from('agent_runs').update({ output })", 'direct agent enriched output persistence')
requireText(handoffRoute, 'enrichOutputWithAIGovernanceIntelligence', 'handoff certification/ROI intelligence enrichment')
requireText(handoffRoute, 'output,', 'handoff enriched output persistence')
requireText(scheduledWorker, 'refreshAllAIGovernanceIntelligence', 'scheduled certification/ROI intelligence refresh')
requireText(scheduledWorker, 'aiGovernanceIntelligence', 'worker exposes refreshed AI governance intelligence evidence')

requireText(fieldLineage, 'run_synthetic_field_lineage_integration_suite', 'self-cleaning field-lineage integration suite')
requireText(fieldLineage, "'column_mappings_complete'", 'field-lineage mapping assertion')
requireText(fieldLineage, 'search_field_lineage_anchors', 'field-level lineage search assertion')
requireText(fieldLineage, "'self_cleaning',true", 'field-lineage cleanup evidence')
requireText(fieldLineage, 'delete from app.organizations', 'field-lineage synthetic cleanup')

requireText(contractRename, 'evaluate_data_contract_for_contract', 'unambiguous contract-specific evaluator name')
requireText(contractRename, 'governance.evaluate_data_contract_for_contract(v_contract_id,p_profile_run_id)', 'automatic contract evaluation delegation')
requireText(contractRename, "'DATA_CONTRACT_FAILED'", 'contract failure certification invalidation')
requireText(contractRename, 'category,severity,title,description,fingerprint,evidence,status', 'contract failure observability alert')

requireText(metricEngine, 'completenessMissingRate', 'blank-aware profiling completeness')
requireText(metricEngine, 'whitespace_only_count', 'blank/whitespace profiling evidence')
requireText(metricEngine, 'result.completeness_rate', 'global score uses blank-aware completeness')

requireText(classificationSync, "existing.status!=='SUGGESTED'", 'AI classification refresh preserves human decisions')
requireText(classificationSync, 'preservedHumanDecisions', 'classification sync reports preserved human decisions')
requireText(reviewProvenance, 'review_dataset_classification', 'classification review RPC')
requireText(reviewProvenance, 'review_cde_mapping', 'CDE mapping review RPC')
requireText(reviewProvenance, 'reviewed_by', 'reviewer provenance')
requireText(reviewProvenance, 'reviewed_at', 'review timestamp provenance')
requireText(reviewProtection, 'trg_protect_classification_human_review', 'classification human-review protection trigger')
requireText(reviewProtection, 'trg_protect_cde_human_review', 'CDE human-review protection trigger')
requireText(reviewProtection, "and new.status='SUGGESTED'", 'AI downgrade protection condition')
requireText(reviewAtomic, 'p_reviewer is null', 'review RPC rejects anonymous/null reviewer')
requireText(reviewAtomic, "'GOVERNANCE_KNOWLEDGE_REVIEW_DECIDED'", 'review decision audit event')
requireText(reviewAtomic, "'atomic_with_decision',true", 'review and audit are transactionally coupled')
requireText(reviewAtomic, "'audit_atomic',true", 'review RPC confirms atomic audit')
requireText(reviewRoute, "'classification.review'", 'classification review authorization')
requireText(reviewRoute, "'stewardship.manage'", 'CDE review authorization')
requireText(reviewRoute, 'data.audit_atomic !== true', 'review API requires atomic audit confirmation')

requireText(enterpriseKnowledgeApproval, 'review_status text not null', 'enterprise knowledge explicit review state')
requireText(enterpriseKnowledgeApproval, 'trg_protect_knowledge_document_review', 'enterprise knowledge direct-review bypass protection')
requireText(enterpriseKnowledgeApproval, 'Governance knowledge review state may only be changed through the governed review RPC', 'enterprise knowledge review mutation guard')
requireText(enterpriseKnowledgeApproval, 'ingest_governance_knowledge_document', 'atomic enterprise governance intake RPC')
requireText(enterpriseKnowledgeApproval, 'review_governance_knowledge_document', 'enterprise governance human approval RPC')
requireText(enterpriseKnowledgeApproval, "'policy.approve'", 'database policy approval authorization')
requireText(enterpriseKnowledgeApproval, "'catalog.update'", 'database knowledge intake authorization')
requireText(enterpriseKnowledgeApproval, 'revoke insert, update, delete on governance.knowledge_documents from anon, authenticated', 'browser knowledge document write revocation')
requireText(enterpriseKnowledgeApproval, 'revoke insert, update, delete on governance.knowledge_requirements from anon, authenticated', 'browser knowledge requirement write revocation')
requireText(enterpriseKnowledgeApproval, "review_status='APPROVED'", 'formal corpus gate requires approval')
requireText(enterpriseKnowledgeApproval, "d.review_status='APPROVED'", 'lexical document search approval boundary')
requireText(enterpriseKnowledgeApproval, 'join governance.knowledge_documents d on d.id=r.document_id', 'lexical requirement parent-document boundary')
requireText(enterpriseRequirementPriority, "('LOW','MEDIUM','HIGH','CRITICAL')", 'enterprise requirement priority vocabulary')
requireText(enterpriseIngestRoute, "authorizeProject(user.id, projectId, 'catalog.update')", 'enterprise knowledge intake API authorization')
requireText(enterpriseIngestRoute, "rpc('ingest_governance_knowledge_document'", 'enterprise knowledge intake API RPC')
requireText(enterpriseIngestRoute, "data.review_status !== 'PENDING'", 'enterprise intake API requires pending review')
requireText(reviewRoute, "'KNOWLEDGE_DOCUMENT'", 'knowledge document review API support')
requireText(reviewRoute, "'policy.approve'", 'knowledge document review API authorization')
requireText(reviewRoute, "'review_governance_knowledge_document'", 'knowledge document review API RPC')
requireText(semanticKnowledgeIndexer, 'isEligibleKnowledgeDocument', 'semantic enterprise knowledge approval boundary')
requireText(semanticKnowledgeIndexer, "document.review_status === 'APPROVED'", 'semantic document approval requirement')
requireText(semanticKnowledgeIndexer, 'documentById.has(requirement.document_id)', 'semantic requirement parent-document approval boundary')

requireText(certificationIntelligence, 'certification_readiness', 'certification readiness intelligence')
requireText(certificationIntelligence, 'governance_roi_snapshots', 'governance value/ROI evidence')
requireText(certificationIntelligence, 'Financial ROI is not estimated', 'ROI anti-fabrication limitation')
requireText(intelligenceLoader, 'loadProjectAIGovernanceIntelligence', 'agent-consumable certification/ROI evidence loader')
requireText(intelligenceLoader, 'enrichOutputWithAIGovernanceIntelligence', 'shared agent intelligence enrichment helper')

requireText(dueDiligenceGate, 'verify_ai_governance_intelligence', 'executable AI governance due diligence RPC')
requireText(dueDiligenceGate, "'SEMANTIC_EMBEDDING_SERVICE_NOT_CONFIGURED'", 'semantic external blocker classification')
requireText(dueDiligenceGate, "'REAL_FIELD_LINEAGE_DATA_NOT_INGESTED'", 'real field-lineage data blocker classification')
requireText(dueDiligenceGate, "'REAL_GOVERNANCE_CORPUS_NOT_INGESTED'", 'real governance corpus blocker classification')
requireText(activationGate, "'NOT_ASSESSED'", 'inactive-project due diligence state')
requireText(activationGate, "'steward_agent'", 'governance-specialist activation detection')
requireText(activationGate, "'support_agent'", 'governance-specialist activation detection completeness')

requireText(semanticJobs, 'GOVERNANCE_EMBEDDING_URL', 'explicit semantic embedding configuration boundary')
requireText(semanticJobs, 'configured: false, queued: 0, projects: 0, skipped: true', 'visible semantic scheduling skip state')

console.log('AI governance due diligence source contracts verified.')
