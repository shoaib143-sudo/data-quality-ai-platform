import { NextResponse } from 'next/server'
import { requireUser } from '@/lib/auth/require-user'
import { createAdminClient } from '@/lib/supabase/admin'
function text(v:unknown){return typeof v==='string'?v.trim():''}
async function access(projectId:string,userId:string){const admin=createAdminClient();const {data:p}=await admin.schema('app').from('projects').select('organization_id').eq('id',projectId).maybeSingle();if(!p)return null;const {data:m}=await admin.schema('app').from('organization_members').select('role').eq('organization_id',p.organization_id).eq('user_id',userId).maybeSingle();return m?admin:null}
export async function POST(request:Request){const user=await requireUser();const b=await request.json();const projectId=text(b.projectId),name=text(b.name),channelType=text(b.channelType).toUpperCase(),target=text(b.target);if(!projectId||!name||!['EMAIL','SLACK','WEBHOOK'].includes(channelType)||!target)return NextResponse.json({error:'Complete channel configuration is required.'},{status:400});const admin=await access(projectId,user.id);if(!admin)return NextResponse.json({error:'Access denied.'},{status:403});const persistedTarget=channelType==='EMAIL'?target:(text(b.displayTarget)||channelType);const {data,error}=await admin.schema('profiling').from('notification_channels').insert({project_id:projectId,name,channel_type:channelType,target:persistedTarget,enabled:true,suppression_minutes:Math.max(0,Number(b.suppressionMinutes??60)),metadata:b.metadata??{}}).select('*').single();if(error||!data)return NextResponse.json({error:error?.message??'Unable to create channel.'},{status:400});if(channelType!=='EMAIL'){const {error:secretError}=await admin.schema('profiling').rpc('store_notification_secret',{p_channel_id:data.id,p_secret:target});if(secretError){await admin.schema('profiling').from('notification_channels').delete().eq('id',data.id);return NextResponse.json({error:`Unable to secure endpoint: ${secretError.message}`},{status:400})}}return NextResponse.json({channel:{...data,target:channelType==='EMAIL'?target:'ENCRYPTED_ENDPOINT'}},{status:201})}
