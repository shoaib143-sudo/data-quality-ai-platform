import { NextResponse } from 'next/server'
import { requireUser } from '@/lib/auth/require-user'
import { createAdminClient } from '@/lib/supabase/admin'
export async function POST(request:Request){const user=await requireUser();const b=await request.json();const projectId=String(b.projectId??''),channelId=String(b.channelId??'');if(!projectId||!channelId)return NextResponse.json({error:'projectId and channelId are required.'},{status:400});const admin=createAdminClient();const {data:p}=await admin.schema('app').from('projects').select('organization_id').eq('id',projectId).maybeSingle();const {data:m}=p?await admin.schema('app').from('organization_members').select('role').eq('organization_id',p.organization_id).eq('user_id',user.id).maybeSingle():{data:null};if(!m)return NextResponse.json({error:'Access denied.'},{status:403});const {data,error}=await admin.schema('profiling').from('notification_routes').insert({project_id:projectId,channel_id:channelId,alert_category:typeof b.alertCategory==='string'&&b.alertCategory?b.alertCategory:null,min_severity:String(b.minSeverity??'MEDIUM').toUpperCase(),dataset_id:b.datasetId||null,enabled:true,escalation_after_minutes:Number.isFinite(Number(b.escalationAfterMinutes))?Number(b.escalationAfterMinutes):null}).select('*').single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({route:data},{status:201})}
