import { NextResponse } from 'next/server'
import { requireUser } from '@/lib/auth/require-user'
import { createAdminClient } from '@/lib/supabase/admin'
import { writeGovernanceAudit } from '@/lib/governance/audit'
export async function PATCH(request:Request,{params}:{params:Promise<{datasetId:string}>}){const user=await requireUser();const {datasetId}=await params;const admin=createAdminClient();const {data:dataset}=await admin.schema('catalog').from('datasets').select('id,project_id').eq('id',datasetId).maybeSingle();if(!dataset)return NextResponse.json({error:'Dataset not found.'},{status:404});const {data:p}=await admin.schema('app').from('projects').select('organization_id').eq('id',dataset.project_id).maybeSingle();const {data:m}=p?await admin.schema('app').from('organization_members').select('role').eq('organization_id',p.organization_id).eq('user_id',user.id).maybeSingle():{data:null};if(!m)return NextResponse.json({error:'Access denied.'},{status:403});const b=await request.json();const payload={project_id:dataset.project_id,dataset_id:datasetId,freshness_sla_hours:Math.max(1,Number(b.freshnessSlaHours??24)),max_volume_change_ratio:Math.max(0,Number(b.maxVolumeChangeRatio??0.5)),max_score_drop:Math.min(1,Math.max(0,Number(b.maxScoreDrop??0.1))),schema_change_policy:String(b.schemaChangePolicy??'ALERT').toUpperCase(),enabled:b.enabled!==false,metadata:b.metadata??{},updated_at:new Date().toISOString()};const {data,error}=await admin.schema('profiling').from('observability_policies').upsert(payload,{onConflict:'dataset_id'}).select('*').single();if(error)return NextResponse.json({error:error.message},{status:400});await writeGovernanceAudit({projectId:dataset.project_id,actorUserId:user.id,eventType:'OBSERVABILITY_POLICY_UPDATED',entityType:'DATASET',entityId:datasetId,metadata:payload});await admin.schema('profiling').rpc('refresh_observability_freshness_alerts');return NextResponse.json({policy:data})}
