import Link from 'next/link'
import { Wrench, Layers3 } from 'lucide-react'
import { requireUser } from '@/lib/supabase/auth'
import { createClient } from '@/lib/supabase/server'
import { IssueManager } from './issue-manager'
export default async function IssuesPage(){await requireUser();const supabase=await createClient();const [projects,datasets,issues,members]=await Promise.all([supabase.schema('app').from('projects').select('id,name,organization_id').order('name'),supabase.schema('catalog').from('datasets').select('id,project_id,name').order('name'),supabase.schema('governance').from('issues').select('*,issue_comments(*)').order('created_at',{ascending:false}),supabase.schema('app').from('organization_members').select('organization_id,user_id,role').order('created_at')]);for(const r of [projects,datasets,issues,members])if(r.error)throw new Error(r.error.message);return <main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8"><nav className="mb-6 flex items-center justify-between rounded-2xl border bg-white px-5 py-3 shadow-sm"><Link href="/dashboard" className="flex items-center gap-3 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white"><Layers3 className="h-5 w-5"/></span>Data Governance PowerHouse</Link><Link href="/data-quality" className="text-sm font-semibold text-blue-600">Data Quality</Link></nav><header className="rounded-3xl border border-amber-100 bg-white p-7 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-amber-50 text-amber-600"><Wrench className="h-6 w-6"/></span><div><h1 className="text-3xl font-black">Issue & Remediation</h1><p className="mt-1 text-sm text-slate-500">Triage governance findings into owned, time-bound remediation with evidence and resolution history.</p></div></div></header><IssueManager projects={projects.data??[]} datasets={datasets.data??[]} members={members.data??[]} initialIssues={issues.data??[]}/></div></main>}
