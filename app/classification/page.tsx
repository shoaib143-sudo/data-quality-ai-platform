import Link from 'next/link'
import { ShieldCheck, Layers3 } from 'lucide-react'
import { requireUser } from '@/lib/supabase/auth'
import { createClient } from '@/lib/supabase/server'
import { ClassificationManager } from './classification-manager'
export default async function ClassificationPage(){await requireUser();const supabase=await createClient();const [projects,datasets,labels,classifications,policies]=await Promise.all([supabase.schema('app').from('projects').select('id,name').order('name'),supabase.schema('catalog').from('datasets').select('id,project_id,name').order('name'),supabase.schema('governance').from('classification_labels').select('*').eq('enabled',true).order('code'),supabase.schema('governance').from('dataset_classifications').select('*,classification_labels(*)').order('created_at',{ascending:false}),supabase.schema('governance').from('classification_policies').select('*,classification_labels(*)').order('created_at',{ascending:false})]);for(const r of [projects,datasets,labels,classifications,policies])if(r.error)throw new Error(r.error.message);return <main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8"><nav className="mb-6 flex items-center justify-between rounded-2xl border bg-white px-5 py-3 shadow-sm"><Link href="/dashboard" className="flex items-center gap-3 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white"><Layers3 className="h-5 w-5"/></span>Data Governance PowerHouse</Link><Link href="/catalog" className="text-sm font-semibold text-blue-600">Catalog</Link></nav><header className="rounded-3xl border border-purple-100 bg-white p-7 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-purple-50 text-purple-600"><ShieldCheck className="h-6 w-6"/></span><div><h1 className="text-3xl font-black">Classification & Policy</h1><p className="mt-1 text-sm text-slate-500">Approve PII, PHI and PCI classifications and enforce handling requirements.</p></div></div></header><ClassificationManager projects={projects.data??[]} datasets={datasets.data??[]} labels={labels.data??[]} initialClassifications={classifications.data??[]} initialPolicies={policies.data??[]}/></div></main>}
