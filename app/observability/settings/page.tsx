import Link from 'next/link'
import { BellRing, Gauge, Layers3 } from 'lucide-react'
import { requireUser } from '@/lib/supabase/auth'
import { createClient } from '@/lib/supabase/server'
import { ObservabilitySettings } from './observability-settings'
export default async function ObservabilitySettingsPage(){await requireUser();const supabase=await createClient();const [projects,datasets,policies,channels,routes,deliveries]=await Promise.all([supabase.schema('app').from('projects').select('id,name').order('name'),supabase.schema('catalog').from('datasets').select('id,project_id,name').order('name'),supabase.schema('profiling').from('observability_policies').select('*'),supabase.schema('profiling').from('notification_channels').select('id,project_id,name,channel_type,target,enabled,suppression_minutes,created_at').order('created_at',{ascending:false}),supabase.schema('profiling').from('notification_routes').select('*').order('created_at',{ascending:false}),supabase.schema('profiling').from('notification_deliveries').select('id,alert_id,channel_id,status,response_code,error_message,delivered_at,created_at').order('created_at',{ascending:false}).limit(100)]);for(const r of [projects,datasets,policies,channels,routes,deliveries])if(r.error)throw new Error(r.error.message);return <main className="min-h-screen bg-slate-50"><div className="mx-auto max-w-7xl px-4 py-6 sm:px-6 lg:px-8"><nav className="mb-6 flex items-center justify-between rounded-2xl border bg-white px-5 py-3 shadow-sm"><Link href="/dashboard" className="flex items-center gap-3 font-bold"><span className="grid h-9 w-9 place-items-center rounded-xl bg-blue-600 text-white"><Layers3 className="h-5 w-5"/></span>Data Governance PowerHouse</Link><Link href="/observability" className="text-sm font-semibold text-blue-600">Observability</Link></nav><header className="rounded-3xl border border-blue-100 bg-white p-7 shadow-sm"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-blue-600"><Gauge className="h-6 w-6"/></span><div><h1 className="text-3xl font-black">Observability Policies & Notifications</h1><p className="mt-1 text-sm text-slate-500">Configure per-dataset SLAs, drift thresholds, notification routing, suppression and escalation.</p></div></div></header><ObservabilitySettings projects={projects.data??[]} datasets={datasets.data??[]} initialPolicies={policies.data??[]} initialChannels={channels.data??[]} initialRoutes={routes.data??[]} initialDeliveries={deliveries.data??[]}/></div></main>}
