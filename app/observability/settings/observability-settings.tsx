'use client'
import { FormEvent,useMemo,useState } from 'react'
import { BellRing,Loader2,Plus,Save } from 'lucide-react'
type Project={id:string;name:string};type Dataset={id:string;project_id:string;name:string};type Policy={id:string;project_id:string;dataset_id:string;freshness_sla_hours:number;max_volume_change_ratio:number;max_score_drop:number;schema_change_policy:string;enabled:boolean};type Channel={id:string;project_id:string;name:string;channel_type:string;target:string;enabled:boolean;suppression_minutes:number};type Route={id:string;project_id:string;channel_id:string;alert_category:string|null;min_severity:string;dataset_id:string|null;enabled:boolean;escalation_after_minutes:number|null};type Delivery={id:string;alert_id:string;channel_id:string|null;status:string;response_code:number|null;error_message:string|null;delivered_at:string|null;created_at:string}
export function ObservabilitySettings({projects,datasets,initialPolicies,initialChannels,initialRoutes,initialDeliveries}:{projects:Project[];datasets:Dataset[];initialPolicies:Policy[];initialChannels:Channel[];initialRoutes:Route[];initialDeliveries:Delivery[]}){const[policies,setPolicies]=useState(initialPolicies);const[channels,setChannels]=useState(initialChannels);const[routes,setRoutes]=useState(initialRoutes);const[projectId,setProjectId]=useState(projects[0]?.id??'');const projectDatasets=datasets.filter(d=>d.project_id===projectId);const[channelName,setChannelName]=useState('Governance alerts');const[channelType,setChannelType]=useState('SLACK');const[target,setTarget]=useState('');const[suppression,setSuppression]=useState('60');const[routeChannel,setRouteChannel]=useState('');const[routeDataset,setRouteDataset]=useState('');const[routeCategory,setRouteCategory]=useState('');const[minSeverity,setMinSeverity]=useState('MEDIUM');const[escalation,setEscalation]=useState('0');const[busy,setBusy]=useState(false);const[message,setMessage]=useState('');const policyByDataset=useMemo(()=>new Map(policies.map(p=>[p.dataset_id,p])),[policies]);
async function savePolicy(datasetId:string,form:HTMLFormElement){setBusy(true);try{const fd=new FormData(form);const r=await fetch(`/api/observability/policies/${datasetId}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({freshnessSlaHours:Number(fd.get('freshness')),maxVolumeChangeRatio:Number(fd.get('volume')),maxScoreDrop:Number(fd.get('score')),schemaChangePolicy:String(fd.get('schema')),enabled:true})});const p=await r.json();if(!r.ok)throw new Error(p.error??'Policy save failed.');setPolicies(v=>[...v.filter(i=>i.dataset_id!==datasetId),p.policy]);setMessage('Observability policy saved.')}catch(e){setMessage(e instanceof Error?e.message:'Policy save failed.')}finally{setBusy(false)}}
async function createChannel(e:FormEvent){e.preventDefault();setBusy(true);try{const r=await fetch('/api/observability/notifications/channels',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId,name:channelName,channelType,target,suppressionMinutes:Number(suppression),displayTarget:channelType==='SLACK'?'Slack webhook':'Webhook endpoint'})});const p=await r.json();if(!r.ok)throw new Error(p.error??'Channel creation failed.');setChannels(v=>[p.channel,...v]);setRouteChannel(p.channel.id);setTarget('');setMessage('Notification channel created securely.')}catch(e){setMessage(e instanceof Error?e.message:'Channel creation failed.')}finally{setBusy(false)}}
async function createRoute(){setBusy(true);try{const channelId=routeChannel||channels.find(c=>c.project_id===projectId)?.id;if(!channelId)throw new Error('Create or select a notification channel first.');const r=await fetch('/api/observability/notifications/routes',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({projectId,channelId,datasetId:routeDataset||null,alertCategory:routeCategory||null,minSeverity,escalationAfterMinutes:Number(escalation)})});const p=await r.json();if(!r.ok)throw new Error(p.error??'Route creation failed.');setRoutes(v=>[p.route,...v]);setMessage('Notification route created.')}catch(e){setMessage(e instanceof Error?e.message:'Route creation failed.')}finally{setBusy(false)}}
return <div className="mt-6 space-y-6">{message?<p className="rounded-xl border bg-white px-4 py-3 text-sm text-slate-600">{message}</p>:null}<section className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Dataset policies</h2><p className="mt-1 text-sm text-slate-500">Thresholds are applied by the observability evaluator and hourly freshness monitor.</p><div className="mt-5 grid gap-3">{datasets.map(d=>{const p=policyByDataset.get(d.id);return <form key={d.id} onSubmit={e=>{e.preventDefault();void savePolicy(d.id,e.currentTarget)}} className="grid gap-3 rounded-2xl border p-4 md:grid-cols-[1.2fr_repeat(4,0.8fr)_auto] md:items-end"><div><p className="font-bold">{d.name}</p><p className="text-xs text-slate-400">{projects.find(x=>x.id===d.project_id)?.name??'Project'}</p></div><label className="text-xs font-bold">Freshness SLA h<input name="freshness" type="number" min="1" defaultValue={p?.freshness_sla_hours??24} className="mt-1 w-full rounded-lg border px-2 py-2"/></label><label className="text-xs font-bold">Volume Δ ratio<input name="volume" type="number" step="0.05" min="0" defaultValue={p?.max_volume_change_ratio??0.5} className="mt-1 w-full rounded-lg border px-2 py-2"/></label><label className="text-xs font-bold">Score drop<input name="score" type="number" step="0.01" min="0" max="1" defaultValue={p?.max_score_drop??0.1} className="mt-1 w-full rounded-lg border px-2 py-2"/></label><label className="text-xs font-bold">Schema policy<select name="schema" defaultValue={p?.schema_change_policy??'ALERT'} className="mt-1 w-full rounded-lg border px-2 py-2"><option>IGNORE</option><option>ALERT</option><option>BLOCK</option></select></label><button disabled={busy} className="inline-flex items-center gap-1.5 rounded-lg bg-blue-600 px-3 py-2 text-sm font-bold text-white"><Save className="h-4 w-4"/>Save</button></form>})}</div></section><div className="grid gap-6 lg:grid-cols-2"><form onSubmit={createChannel} className="rounded-3xl border bg-white p-6 shadow-sm"><div className="flex items-center gap-2"><BellRing className="h-5 w-5 text-violet-600"/><h2 className="text-xl font-bold">Notification channels</h2></div><div className="mt-4 grid gap-3"><select value={projectId} onChange={e=>setProjectId(e.target.value)} className="rounded-xl border px-3 py-2.5">{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select><input value={channelName} onChange={e=>setChannelName(e.target.value)} className="rounded-xl border px-3 py-2.5"/><select value={channelType} onChange={e=>setChannelType(e.target.value)} className="rounded-xl border px-3 py-2.5"><option>SLACK</option><option>WEBHOOK</option><option>EMAIL</option></select><input value={target} onChange={e=>setTarget(e.target.value)} placeholder={channelType==='EMAIL'?'alerts@example.com':'Webhook URL stored encrypted in Vault'} className="rounded-xl border px-3 py-2.5"/><input type="number" value={suppression} onChange={e=>setSuppression(e.target.value)} className="rounded-xl border px-3 py-2.5" placeholder="Suppression minutes"/><button disabled={busy} className="inline-flex items-center justify-center gap-2 rounded-xl bg-violet-600 px-4 py-3 font-bold text-white">{busy?<Loader2 className="h-4 w-4 animate-spin"/>:<Plus className="h-4 w-4"/>}Create channel</button></div><div className="mt-4 space-y-2">{channels.filter(c=>c.project_id===projectId).map(c=><div key={c.id} className="rounded-xl bg-slate-50 p-3 text-sm"><span className="font-bold">{c.name}</span><span className="ml-2 rounded-full bg-white px-2 py-0.5 text-xs">{c.channel_type}</span><p className="mt-1 text-xs text-slate-500">{c.channel_type==='EMAIL'?c.target:'Encrypted endpoint'} · suppress {c.suppression_minutes}m</p></div>)}</div></form><section className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Routing & escalation</h2><div className="mt-4 grid gap-3"><select value={routeChannel} onChange={e=>setRouteChannel(e.target.value)} className="rounded-xl border px-3 py-2.5"><option value="">Select channel</option>{channels.filter(c=>c.project_id===projectId).map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select><select value={routeDataset} onChange={e=>setRouteDataset(e.target.value)} className="rounded-xl border px-3 py-2.5"><option value="">All datasets</option>{projectDatasets.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select><select value={routeCategory} onChange={e=>setRouteCategory(e.target.value)} className="rounded-xl border px-3 py-2.5"><option value="">All alert categories</option>{['QUALITY_SCORE_DROP','SCHEMA_DRIFT','VOLUME_CHANGE','QUALITY_RULE_FAILURE','PROFILE_FAILURE','FRESHNESS'].map(v=><option key={v}>{v}</option>)}</select><select value={minSeverity} onChange={e=>setMinSeverity(e.target.value)} className="rounded-xl border px-3 py-2.5"><option>INFO</option><option>LOW</option><option>MEDIUM</option><option>HIGH</option><option>CRITICAL</option></select><input type="number" min="0" value={escalation} onChange={e=>setEscalation(e.target.value)} className="rounded-xl border px-3 py-2.5" placeholder="Escalation delay minutes"/><button onClick={()=>void createRoute()} className="rounded-xl bg-blue-600 px-4 py-3 font-bold text-white">Add route</button></div><div className="mt-4 space-y-2">{routes.filter(r=>r.project_id===projectId).map(r=><div key={r.id} className="rounded-xl bg-slate-50 p-3 text-xs"><span className="font-bold">{channels.find(c=>c.id===r.channel_id)?.name??r.channel_id}</span><span className="ml-2">{r.alert_category??'ALL'} · ≥ {r.min_severity} · {r.dataset_id?datasets.find(d=>d.id===r.dataset_id)?.name:'all datasets'} · delay {r.escalation_after_minutes??0}m</span></div>)}</div></section></div><section className="rounded-3xl border bg-white p-6 shadow-sm"><h2 className="text-xl font-bold">Recent deliveries</h2><div className="mt-4 overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr className="border-b text-xs uppercase text-slate-400"><th className="px-2 py-2">Status</th><th className="px-2 py-2">Channel</th><th className="px-2 py-2">Response</th><th className="px-2 py-2">Time</th></tr></thead><tbody>{initialDeliveries.map(d=><tr key={d.id} className="border-b"><td className="px-2 py-2 font-bold">{d.status}</td><td className="px-2 py-2">{channels.find(c=>c.id===d.channel_id)?.name??'N/A'}</td><td className="px-2 py-2 text-xs text-slate-500">{d.response_code??d.error_message??'—'}</td><td className="px-2 py-2 text-xs text-slate-500">{new Date(d.created_at).toLocaleString()}</td></tr>)}</tbody></table></div></section></div>}
