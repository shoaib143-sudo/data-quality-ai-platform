name: Governance Agent Service Role ACL

on:
  pull_request:
    paths:
      - 'supabase/migrations/**'
      - 'scripts/verify-governance-agent-service-role-acl.mjs'
      - '.github/workflows/governance-agent-service-role-acl.yml'
  push:
    branches: [main]
    paths:
      - 'supabase/migrations/**'
      - 'scripts/verify-governance-agent-service-role-acl.mjs'
      - '.github/workflows/governance-agent-service-role-acl.yml'

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify governance-agent service-role ACL
        run: node scripts/verify-governance-agent-service-role-acl.mjs
